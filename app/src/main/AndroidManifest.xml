<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.github.gibbrich.paintfactory">

    <application
            android:name=".PaintShopApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".ui.ColorsCalculationActivity"
                android:label="@string/activity_colors_calculation_label" />
        <activity android:name=".ui.CustomerDetailActivity"
                android:label="@string/activity_customer_detail_label" />
        <activity android:name=".ui.CustomersActivity"
                android:label="@string/activity_customers_label"/>
        <activity android:name=".ui.MainActivity"
                android:label="@string/activity_main_title" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>